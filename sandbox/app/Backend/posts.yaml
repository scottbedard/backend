permissions:
    - read posts

routes:
    index:
        path: /
        page: list
        options:
            checkboxes: true
            schema:
                id:
                    disabled: true
                created_at:
                    format: YYYY-mm-DD
                    type: date
                updated_at:
                    format: YYYY-mm-DD
                    type: date

    create:
        path: /create
        permissions:
            - create post
        page: form
        options:
            context: create
            fields:
                id:
                    disabled: true

    edit:
        path: /{id}/edit
        permissions:
            - edit post
        page: form
        options:
            context: edit
            extends: create
            fields:
                id:
                    disabled: false

