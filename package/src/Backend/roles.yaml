id: roles

permissions:
    - read roles

routes:
    index:
        page: list
        path: /
        options:
            checkboxes: true
            schema:
                id:
                    disabled: true
                created_at:
                    format: YYYY-mm-DD
                    type: date
                updated_at:
                    format: YYYY-mm-DD
                    type: date

    create:
        page: form
        path: /create
        permissions:
            - create roles
        options:
            context: create
            fields:
                id:
                    disabled: false

    edit:
        page: form
        path: /{id}/edit
        permissions:
            - edit role
        options:
            context: edit
            extends: create
            fields:
                created_at:
                    disabled: true

